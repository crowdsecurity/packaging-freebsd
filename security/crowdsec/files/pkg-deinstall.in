#!/bin/sh

case $2 in
"DEINSTALL")
	service crowdsec onestatus >/dev/null 2>&1 && touch /var/run/crowdsec.running
	if service crowdsec enabled >/dev/null 2>&1; then
		service crowdsec stop >/dev/null 2>&1 || :
	else
		service crowdsec onestop >/dev/null 2>&1 || :
	fi
	;;
esac
