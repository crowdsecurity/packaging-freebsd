#!/bin/sh

case $2 in
       "DEINSTALL")
               service crowdsec_spoa_bouncer status 2>/dev/null && touch /var/run/crowdsec_spoa_bouncer.running
               service crowdsec_spoa_bouncer stop 2>/dev/null || :
               ;;
esac

